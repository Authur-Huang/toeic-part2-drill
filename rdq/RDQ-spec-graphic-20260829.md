---
rdq_version: 1
task: 多益 Part 3／4 圖表題（look at the graphic）
domain: dev
date: 2026-08-29
status: confirmed
telemetry:
  mode: lite
  rounds: 1
  questions: 4
  q4_adopted: 5
  revisions: 0
supersedes: RDQ-spec-part34-20260828.md 的「圖表題第一版不做」假設
downstream: self
---

# RDQ 需求規格：Part 3／4 圖表題

## 一句話任務
在 `toeic-part2-drill` PWA 加入 **6 組圖表題（18 題，其中 6 題是圖表題）**，圖表以 JSON／inline SVG 描述、App 端渲染，**不用圖片檔**；並把音檔流程改成貼近正式考試的報題號形式。

## ✅ 已確認
- **6 組 18 題**，每組 3 題、其中**恰 1 題**是圖表題（正式考試每組最多一題）（Ⅲ）
- **這個週末（8/29–8/30）就做**，不等 9 月（Ⅲ）
- **混入＋可篩選**：預設混進 Part 3／4 隨機出現，另加「只練圖表題」開關（Ⅲ）
- 圖表**不用圖片檔**：表格類用 JSON→`<table>`（約 300 B），非表格類用 inline SVG（1–2 KB）（Ⅰ）
- 圖表**必須在播音前就顯示**——正式考試圖表印在題本上（Ⅰ）
- 死線其實是**計畫第 6 週 2026-10-05**（`plan.json` 已排「Part 3 圖表題」週，但題庫是空的）（Ⅰ）

## ❓ 假設（未確認，已採預設值，隨時可推翻）
- 六組分配 → **Part 3 四組、Part 4 兩組**（貼近真考 39:30 的題數比）
- 六種圖表型態 → 時刻表／價目表／庫存訂單表（表格類 3 種）＋ 樓層平面圖／地圖路線／長條圖（SVG 類 3 種）
- 出題鐵律 → **正解字串絕不可出現在對話原文**，否則只是偽裝成圖表題的細節題；`build_site.py` 加自動守門擋下
- 錯題排程 → 沿用現行 1／3／7 天，不另立規則
- 工時 → 約 **1.5–2 天**（含下方五項採納）

## ➕ 已採納（象限Ⅳ，你全勾了）
- **Part 4 也做圖表題** — 代價：題量與音檔加倍
- **加做 SVG 類圖表**（地圖／樓層平面／長條圖）— 代價：每張手繪約 15 分鐘
- **答錯時圖表與逐字稿並排**，標出「該查哪一欄」— 代價：檢討畫面改版，約半天
- **圖表題正確率單獨統計** — 代價：統計邏輯要改，約 1 小時
- **音檔改成多益語音流程：先報第幾題** — 見下方 ⚠

## ⚠ 需要你裁決的一點（你的 Other 只有一半能照做）
你寫：「**會先報第幾題，然後依序選擇項目 A、B、C**」。拆成兩件事：

1. **報題號** → ✅ 完全同意，現在確實沒有。會改成正式考試的講法：
   段落前 `Questions 1 through 3 refer to the following conversation.`，
   每題前 `Number 1.` 再接題目。**Part 2 也一起補上 `Number 8.` 這類報號。**
2. **依序唸出選項 A、B、C** → 🔴 **建議只留在 Part 2，不要加到 Part 3／4。**
   正式考試 **Part 3／4 的選項是印在題本上、不會唸出來的**（只有 Part 1、Part 2 的選項用唸的）。
   你現在 Part 2 已經是唸選項（正確），Part 3／4 是顯示文字（也正確）。
   若把 Part 3／4 的選項也唸出來，等於練成「等人唸選項」的習慣，**考試當天不會有人唸**。

→ **預設照上面做**（報題號全加、Part 3／4 不唸選項）。你要堅持全唸，回覆時說一聲即可。

## ❌ 排除項（明確不做）
- **不**做 Part 1、Part 5／6／7 的圖表／圖片題
- **不**用圖片檔（PNG／JPG）畫圖表
- **不**改 repo 名稱與網址
- **不**碰第三方測驗的題目、音檔或圖表

## 📋 一段式需求規格
在 `C:\Users\USER\toeic-part2-drill`（公開 repo `Authur-Huang/toeic-part2-drill`）既有的 Part 3／4 題庫上，新增 **6 組共 18 題**，其中每組恰含 **1 題圖表題**（合計 6 題），分配為 **Part 3 四組、Part 4 兩組**。圖表以 JSON 描述表格（App 端渲染 `<table>`）或 inline SVG（地圖、樓層平面、長條圖）呈現，**不使用任何圖片檔**，且**必須在音檔播放前就顯示於畫面**。出題鐵律是**正解字串不得出現在對話原文**——聽到的是圖表的某一欄，要答的是另一欄；此規則須在 `build_site.py` 加自動檢查，違反即中止建置。圖表題預設**混入** Part 3／4 隨機出現，另提供「**只練圖表題**」篩選開關供計畫第 6 週集中練習，答錯時**圖表與逐字稿並排顯示**並標出該查哪一欄，成績頁**單獨統計圖表題正確率**。音檔流程改為貼近正式考試：段落前唸 `Questions X through Y refer to the following conversation/talk.`，每題前唸 `Number N.`；**Part 2 亦補上報題號**，但 **Part 3／4 的四個選項維持不唸**（正式考試印在題本）。題目與語音全部自製、Edge-TTS 四種口音，零第三方版權素材。

## ✔ 驗收條件（2026-08-29 實測，七項全過）
- [x] 手機上圖表看得清楚，**播音前就在畫面上**，橫向不溢出
- [x] 6 題圖表題的正解字串，**沒有一題**出現在該段對話原文裡（建置檢查會擋）
- [x] 「只練圖表題」開關能單獨抽出這 6 題
- [x] 音檔會唸 `Questions 1 through 3…` 與 `Number 1.`；Part 3／4 **不唸**選項
- [x] 答錯後的檢討畫面，圖表與逐字稿並排看得到
- [x] 成績頁看得到圖表題的獨立正確率
- [x] `build_site.py` 全部檢查通過，正解分佈仍均衡
